package com.bopr.android.smailer;

import android.content.Context;

import org.junit.Test;

/**
 * {@link PreferencesPermissionsChecker} tester.
 *
 * @author Boris Pronin (<a href="mailto:boprsoft.dev@gmail.com">boprsoft.dev@gmail.com</a>)
 */
public class PreferencesPermissionsCheckerTest extends BaseTest {

    /**
     * Tests {@link PermissionsChecker#isPermissionsDenied(Context, String...)} method.
     *
     * @throws Exception when failed
     */
    @Test
    public void testIsPermissionsDenied() throws Exception {
//        Log.i("TEST", Settings.getPermissionLabel(getContext(), RECEIVE_SMS));
//        Log.i("TEST", Settings.getPermissionLabel(getContext(), READ_PHONE_STATE));
//        Log.i("TEST", Settings.getPermissionLabel(getContext(), PROCESS_OUTGOING_CALLS));
//        Log.i("TEST", Settings.getPermissionLabel(getContext(), READ_CONTACTS));
//        Log.i("TEST", Settings.getPermissionLabel(getContext(), ACCESS_COARSE_LOCATION));
//        Log.i("TEST", Settings.getPermissionLabel(getContext(), ACCESS_FINE_LOCATION));
    }
}